//Author:Eric Perez
#include<iostream>
#include<string>

using namespace std;

int main()
{
  //declare variables
string first;

string last;

int first,last;



//get user input

std::cout << " What is your first name? " << first << endl;
std::cin >> first;
std::cout << " What is your last name? " << last << endl;
std::cin >> last;
cout<<"Welcome, "<<first[0]<<"."<<last[0]<<"."<<" here is your fortune..."<<endl;

//tell fortune
cout<<"Your Lucky Number is "<<first.length()<<endl;

cin>> first;
// evaluate lower case first and last vowel
isLowercaseVowel = (index == 'a' || index == 'e' || index == 'i' || index == 'o' || c == 'u');

    // evaluates if first and last  is an uppercase vowel
    isUppercaseVowel = ( index == 'A' || index == 'E' || index == 'I' || index == 'O' || index == 'U');

    // evaluates if either isLowercaseVowel or isUppercaseVowel is true
    if (first)
        cout << index << " you are destined to be famous.";
     {

     else(cout<< index << " you have already met your true love."
 }

 cout(cout<<have a good day!<<endl;
  return 0;
}
